version: "3.8"

services:
  mongo:
    image: mongo:6
    container_name: eventra
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: Sybil
      MONGO_INITDB_ROOT_PASSWORD: jeni23
      MONGO_INITDB_DATABASE: Eventra38
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
